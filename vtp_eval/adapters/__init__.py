# vtp_eval/adapters/__init__.py
"""lmms-eval adapter classes for visual-token-pruning methods.

Registration into lmms-eval's AVAILABLE_SIMPLE_MODELS happens in
vtp_eval.eval.run_lmms (the CLI entry), not here.
"""
